1. apt install nginx
2. sudo ufw allow 'Nginx Full' 
3. sudo ufw enable   # Only if UFW is actually active on your system (optional)
4. sudo nano /etc/nginx/sites-available/api
server {
    listen 80;
    server_name ip_address;

    location / {
        proxy_pass http://localhost:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

5. sudo ln -s /etc/nginx/sites-available/api /etc/nginx/sites-enabled/
6.sudo nginx -t
7.sudo systemctl reload nginx
8.sudo apt install certbot python3-certbot-nginx
9.create an A tag in ionos
10.sudo certbot --nginx -d ip_address 
11.sudo nano /etc/nginx/sites-available/api.domain
12. server {
    listen 80;
    server_name api.t333scustoms.co.uk;

    location / {
        proxy_pass http://localhost:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
13.sudo ln -s /etc/nginx/sites-available/api.domain /etc/nginx/sites-enabled/
14.sudo nginx -t
15.sudo systemctl reload nginx





