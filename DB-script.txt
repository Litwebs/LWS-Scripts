
	1.sudo rm /etc/apt/sources.list.d/mongodb-org-*
	2.sudo apt-get install -y gnupg curl
	3.curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
	4.echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/8.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
	5.sudo apt-get update
	6.sudo apt-get install -y mongodb-org
	7.sudo systemctl start mongod sudo systemctl enable mongod
	8.sudo systemctl status mongod
	9.mongod --version
	10. sudo nano /etc/mongod.conf
	11. sudo systemctl restart mongod
	12. mongosh

	Use admin 
	db.createUser({
  		user: "Admin",
  		pwd: "w0nH5aYLbFGiULAG0JcLuA4OQY5oOXZjzaUdcZdQ22EZSy69Fy",
	  	roles: [{ role: "root", db: "admin" }]
	})

	db.auth("Admin", "")

mongosh -u Admin -p 'w0nH5aYLbFGiULAG0JcLuA4OQY5oOXZjzaUdcZdQ22EZSy69Fy' -authenticationDatabase admin
db.runCommand({ connectionStatus: 1 })
use T3DB
db.createUser({
    user: "T3AppUser",
    pwd: "AppUserPassword123!",
    roles: [{ role: "readWrite", db: "T3DB" }]
})

sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo systemctl restart mongod
sudo systemctl status mongod

sudo nano /lib/systemd/system/mongod.service

#In services 
Restart=on-failure
RestartSec=3s

sudo systemctl daemon-reload
sudo systemctl restart mongod



